services:
  playwright:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./reports:/app/reports
      - ./allure-results:/app/allure-results
      - ./test-results:/app/test-results
    # Optional local overrides; CI=false keeps the html reporter output
    env_file:
      - path: .env
        required: false
